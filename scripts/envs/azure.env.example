# ─────────────────────────────────────────────────────────────────────────────
# CSB Azure Brokerpak — Environment Variables
# Copy this file to scripts/envs/azure.env and fill in your values.
# NEVER commit scripts/envs/azure.env — it is git-ignored.
#
# CREDENTIAL PROVISIONING GUIDE (brew install → auth → service principal → this file):
#   docs/credential-provisioning.md  §  Azure — azd + az
# ─────────────────────────────────────────────────────────────────────────────

# ── Broker Identity ────────────────────────────────────────────────────────────────
SECURITY_USER_NAME=csb-azure-broker
SECURITY_USER_PASSWORD=CHANGEME-strong-password

# ── CF App Name & Broker Registration ──────────────────────────────────────────
APP_NAME=csb-azure
BROKER_NAME=csb-azure-sandbox
MYSQL_INSTANCE=csb-sql

# ── Azure Service Principal Credentials ──────────────────────────────────────────
# Provided by TechOps for the sandbox Azure subscription.
# The SP needs Contributor + User Access Administrator on the sandbox subscription,
# scoped only to the dedicated sandbox resource group where applicable.
ARM_TENANT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
ARM_SUBSCRIPTION_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
ARM_CLIENT_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
ARM_CLIENT_SECRET=CHANGEME-sp-secret

# Optional: override default location (default eastus)
# ARM_LOCATION=eastus

# Optional: budget sidecar for Azure OpenAI instances.
# This is alerting / optional webhook automation, not a hard real-time spend cap.
# AZURE_BUDGET_CONTACT_EMAIL=operator@example.gov
# AZURE_BUDGET_WEBHOOK_URL=https://example.gov/hooks/azure-openai-budget-enforcer

# ── Optional Plan Overrides ──────────────────────────────────────────────────
# The current Azure brokerpak branch exposes these services:
#   csb-azure-mongodb
#   csb-azure-mssql-db
#   csb-azure-mssql-db-failover-group
#   csb-azure-mssql-fog-run-failover
#   csb-azure-redis
#   csb-azure-openai
#
# If you need to override a service's plans locally, use the broker's standard
# override key format and JSON arrays, for example:
#   GSB_SERVICE_CSB_AZURE_MSSQL_DB_PLANS='[...]'
#   GSB_SERVICE_CSB_AZURE_REDIS_PLANS='[...]'
#
# Azure OpenAI plans are already defined in the checked-out feature branch.
# Do not override them locally unless you also change the built-in service YAML.

# Optional: run an Azure OpenAI model preflight before broker build/push.
# This calls scripts/azure-openai-preflight.sh and can probe the same
# create path Terraform uses.
# Keep the JSON value shell-quoted in this .env file so `source` preserves it.
# AZURE_OPENAI_PREFLIGHT=1
# AZURE_OPENAI_PREFLIGHT_LOCATION=eastus
# AZURE_OPENAI_PREFLIGHT_DEPLOYMENTS_JSON='[{"name":"gpt-5.5","model":"gpt-5.5","version":"2026-04-24","capacity":10}]'
# AZURE_OPENAI_PREFLIGHT_SKU_NAME=Standard
# AZURE_OPENAI_PREFLIGHT_PROBE_CREATE=1
# Optional explicit probe account instead of auto-discovery:
# AZURE_OPENAI_PREFLIGHT_RESOURCE_GROUP=csb-openai-full-0505a
# AZURE_OPENAI_PREFLIGHT_ACCOUNT_NAME=csb213089a5e4244179bbf59

# ── Global Provision Defaults ─────────────────────────────────────────────────────
# Keep this to cross-service defaults that are safe for all Azure offerings.
# Do not set resource_group globally; AI services now manage that per instance.
GSB_PROVISION_DEFAULTS='{"location": "eastus"}'

# ── CredHub (optional) ────────────────────────────────────────────────────────────────
# CH_CRED_HUB_URL=https://credhub.service.cf.internal:8844
# CH_UAA_URL=https://uaa.service.cf.internal:8443
# CH_UAA_CLIENT_NAME=credhub_admin_client
# CH_UAA_CLIENT_SECRET=CHANGEME
