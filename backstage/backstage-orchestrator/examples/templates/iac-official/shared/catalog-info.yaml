apiVersion: backstage.io/v1alpha1
kind: Resource
metadata:
  name: ${{ values.name }}
  description: ${{ values.description }}
spec:
  type: resource
  lifecycle: development
  owner: ${{ values.owner }}