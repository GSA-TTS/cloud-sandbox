# AZURE Approved Service Conditions

Generated from in-house tracking list on 2026-04-14.

For each approved service, document usage caveats, configuration requirements, protocols, and settings.

## Virtual Machines

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Cloud Services

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Azure SQL Database

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Azure Active Directory

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Multi-Factor Authentication

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Traffic Manager

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## VPN Gateway

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Virtual Network

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Load Balancer

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Application Gateway

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Key Vault

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Storage (including cool and premium)

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Event Hubs

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Azure Analysis Services

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## HDInsight

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Power BI

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Functions

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Service Fabric

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## App Service

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Batch

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Virtual Machine Scale Sets

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## App Service

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## SQL Data Warehouse

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Redis Cache

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Azure Cosmos DB

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## SQL Server Stretch Database

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Table Storage (including cool and premium)

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Azure DevTest Labs

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## API Management

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Service Bus

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## IoT Hub

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Notification Hubs

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Logic Apps

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Backup

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Scheduler

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Azure Monitor

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Azure Resource Manager

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Site Recovery

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Automation

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Log Analytics (Including Network Performance Monitor)

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Network Watcher

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Microsoft Azure Portal

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Media Services

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## App Service

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## API Apps

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Azure DNS

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## ExpressRoute

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Azure Information Protection

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## StorSimple

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Blob Storage (including cool and premium)

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## File Storage(including cool and premium)

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Disk Storage (including cool and premium)

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Queue Storage (including cool and premium)

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## App Service

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## App Service

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## App Service Linux

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## App Service Linux

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Expressv2

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Import/Export

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Intune

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Marketplace

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Workflow

- In-house status: `FedRAMP Authorized - Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Machine Learning Services

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Databricks

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Azure Bot Service

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Cognitive Services

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Content Moderator

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Custom Vision

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Face

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud and Azure Government`
- Conditions / notes: https://marketplace.fedramp.gov/#/product/azure-commercial-cloud/versus/azure-government?sort=productName&productNameSearch=azure
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## Video Indexer

- In-house status: `FedRAMP Authorized - Azure Commercial Cloud`
- Conditions / notes: No explicit caveats in source list; apply baseline controls from SSP/OSCAL, least privilege IAM, private networking, encryption in transit/at rest, and required sandbox tags.
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.

## DevOps Services

- In-house status: `Approved with caveats`
- Conditions / notes: https://docs.google.com/document/d/10Il4Rn1EAKaTyRAdrw_FsUVWVRhZFwr-OHVzB7HbInk/edit
- Required baseline configuration:
  - Use non-production sandbox plans only (no HA / multi-AZ unless explicitly approved).
  - Enforce tagging: `Project`, `Owner`, `TTLExpiry`, `CostCenter`, `Cloud`, `Environment`.
  - Restrict public exposure; prefer private endpoints/network controls where available.
  - Log all access and changes for audit traceability.
